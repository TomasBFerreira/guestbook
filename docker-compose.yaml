# docker-compose.yaml
version: '3'
services:
    database:
        image: mysql:8.0.17
        environment:
            MYSQL_ROOT_PASSWORD: main
            MYSQL_DATABASE: main
        ports: [5432]